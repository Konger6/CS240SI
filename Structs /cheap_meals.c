/* cheap_meals.c - file to struct */
/* Made by Sophia Konger */

#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <string.h>

#define MAX_FOOD_ITEMS[15];

/*
 * INSTRUCTIONS:
 * Create a struct, foodItem_data_t, of typedef, foodItem, with variables: int calories, int fat
 * int carbs, float protein, float price
 */

FoodItem g_data_arr[MAX_FOOD_ITEMS];
int g_num_items = 0;

/*---------------------------------------------------------------
 * reads data from a file, macros.txt, and stores it in a global Food array
 */--------------------------------------------------------------

int read_data(char *filename) {
    return 1;
}

/*---------------------------------------------------------------
 * records all the entries in order of cost, from cheapest to most expensive
 *---------------------------------------------------------------
 */

void filter_by_cost() {
  
}

/*---------------------------------------------------------------
 * writes all the entries in the exact same way it shows in original file, but filtered by cost
 *---------------------------------------------------------------
 */

int write_data(char *filename) {
}

int main() {
}
 
